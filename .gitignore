<<<<<<< HEAD
.gitignore
#Dependencies
#This directory contains all third-party packages installed via npm.
#It can be very large and is always regenerated by running npm install.
#Committing this would bloat the repository unnecessarily.
/node_modules

#Environment Variables & Secrets
#This file contains sensitive information like API keys and database credentials.
#It MUST NEVER be committed to version control for security reasons.
#Each developer and server should have its own local .env file.
.env
.env.*
!.env.example

#Logs
#Log files are generated during runtime and vary between environments and sessions.
#They are for debugging, not for the project's history.
.log
*.log
/logs
/temp
npm-debug.log
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

#Temporary Files & Runtime Data
#This includes temporary system files and data generated by the application during a run,
#such as converted images, PDFs, and JSON output.
/tmp
/temp
*.tmp
*.temp
*.pdf
*.png
*.jpeg
.jpg
output.json
output-.json
*.json
#IDE / Editor Directories
#These files are specific to a developer's local environment and editor settings.
#Including them would cause conflicts and clutter for other team members.
.idea/
.vscode/
*.suo
.ntvs
*.njsproj
*.sln
*.sw?

#OS-generated files
#These are system files created by macOS, Windows, and Linux that are not
#relevant to the project's source code.
.DS_Store
=======
.gitignore
#Dependencies
#This directory contains all third-party packages installed via npm.
#It can be very large and is always regenerated by running npm install.
#Committing this would bloat the repository unnecessarily.
/node_modules

#Environment Variables & Secrets
#This file contains sensitive information like API keys and database credentials.
#It MUST NEVER be committed to version control for security reasons.
#Each developer and server should have its own local .env file.
.env
.env.*
!.env.example

#Logs
#Log files are generated during runtime and vary between environments and sessions.
#They are for debugging, not for the project's history.
.log
*.log
/logs
npm-debug.log
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

#Temporary Files & Runtime Data
#This includes temporary system files and data generated by the application during a run,
#such as converted images, PDFs, and JSON output.
/tmp
/temp
*.tmp
*.temp
*.pdf
*.png
*.jpeg
.jpg
output.json
output-.json
*.json
#IDE / Editor Directories
#These files are specific to a developer's local environment and editor settings.
#Including them would cause conflicts and clutter for other team members.
.idea/
.vscode/
*.suo
.ntvs
*.njsproj
*.sln
*.sw?

#OS-generated files
#These are system files created by macOS, Windows, and Linux that are not
#relevant to the project's source code.
.DS_Store
>>>>>>> 6ec2fa46af6c6cbadd8ee0d5759e8f3464f292b0
Thumbs.db